import React, { Component }  from 'react';

class Footer extends Component {
    render() {
        return(
            <h1 className="Heading">Some text at the bottom</h1>
        );
    }
}

export default Footer;